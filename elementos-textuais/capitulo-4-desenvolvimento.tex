\chapter{Desenvolvimento}

Neste capítulo, são apresentados os detalhes do desenvolvimento do projeto, com foco nos encaminhamentos técnicos adotados, nos desafios enfrentados e nos resultados obtidos.

\section{Cenário preexistente}

O Programa Municipal de Aleitamento Materno de Osório contava com uma equipe multiprofissional, além de profissionais de referência atuando em cada Estratégia de Saúde da Família (ESF). Conforme documentado por \cite{silva2018painelpromama}, a atuação era predominantemente presencial, envolvendo visitas domiciliares, grupos de orientação e atividades de sala de espera. 

Essa abordagem, embora eficaz no contato direto com a comunidade, restringia-se espacialmente a localidades e horários específicos, o que limitava a abrangência da divulgação das informações ao conjunto de famílias atendidas.

A solução inicial concebida por Lucas buscou estender o alcance do programa mediante o uso de um sistema informatizado integrado a um aplicativo móvel multiplataforma. A concepção desse sistema permitiria à equipe inserir conteúdos e responder dúvidas. Esses conteúdos e respostas seriam transmitidos aos dispositivos dos usuários. O modelo pretendia superar as limitações físicas, atingindo todo o município por meio da comunicação digital, sem abandonar a lógica de encaminhamentos e suporte presencial quando necessário.

No modelo implementado à época, o sistema era composto por três elementos principais: Painel Administrativo, \emph{API} e Banco de Dados. Utilizando o \emph{framework} Laravel (PHP) e arquitetura MVC, o Painel fornecia a \emph{interface} \emph{web} para administração dos conteúdos. A \emph{API}, construída sobre os princípios REST, se comunicava com o banco MySQL e retornava os dados em formato JSON, consumidos pelo aplicativo móvel.

Para a implantação, a Secretaria de Saúde de Osório disponibilizou um servidor virtual com sistema operacional Ubuntu. A configuração do ambiente envolveu a instalação manual dos componentes necessários. A aplicação era então clonada do repositório GitHub e ajustada para execução no servidor. Foi estabelecida comunicação via protocolo HTTP, com a liberação da porta 80 para o tráfego de dados.

Uma das principais limitações do sistema original residia na ausência de criptografia na comunicação, uma vez que o uso do protocolo HTTP sem certificado digital expunha os dados a vulnerabilidades. A instalação manual dos componentes dificultava a replicação da infraestrutura em outros municípios. Além disso, o acoplamento entre \emph{front-end} e \emph{back-end} aumentava a complexidade de manutenção do sistema. A documentação das rotas da \emph{API} era feita manualmente, o que tornava o processo suscetível a erros e dificultava sua atualização.

\section{Estrutura base do projeto}

Esta seção descreve a base técnica do projeto, com ênfase na modelagem do banco de dados e na configuração do ambiente de desenvolvimento. O objetivo é apresentar as definições técnicas que deram suporte à implementação e à manutenção da solução.

\subsection{Modelagem do banco de dados}

A modelagem do banco de dados foi definida a partir da análise de requisitos do sistema, com o objetivo de representar, de forma consistente, os dados necessários ao cadastro, ao acompanhamento materno-infantil, à produção de conteúdo e à interação com as usuárias.

De acordo com \citeonline{elmasri2016fundamentals}, o projeto de banco de dados inicia-se pela identificação de requisitos e restrições do domínio e, posteriormente, pela organização dessas informações em esquemas que expressem entidades, relacionamentos e regras de integridade. Com base nesse entendimento, a modelagem adotada no projeto priorizou coerência semântica e consistência estrutural desde a etapa lógica.

A Figura~\ref{fig:modelo_logico} apresenta o diagrama do modelo lógico elaborado para o Pró-Mamá, contemplando as principais entidades e suas associações. Esse diagrama foi utilizado como referência para verificar a coerência dos relacionamentos antes da implementação física.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{imagens/modelogic4.pdf}
    \caption{Modelo Lógico do Banco de Dados do Pró-Mamá}
    \label{fig:modelo_logico}
    \legend{Fonte: Elaborado pelo autor (2025).}
\end{figure}

Com base nesse diagrama, o modelo físico foi implementado em PostgreSQL por meio de migrações SQL, com definição de chaves primárias, chaves estrangeiras e tipos enumerados para domínios fechados. Essa estrutura fortalece a integridade referencial e reduz ambiguidades no armazenamento das informações.

Além das tabelas centrais do domínio, foram incluídas estruturas de apoio para registro de leituras, histórico de acesso e controle da evolução do esquema. Também foram adotados campos temporais de criação, atualização e, quando aplicável, exclusão lógica, favorecendo rastreabilidade e manutenção.

Desse modo, a modelagem articulou uma visão lógica orientada ao domínio com uma implementação física aderente às regras de negócio, estabelecendo uma base estável para as etapas seguintes de implementação.

\subsection{Configuração do ambiente de desenvolvimento}

A configuração do ambiente foi conduzida com foco na reprodutibilidade das rotinas de desenvolvimento, buscando tornar a execução local mais previsível entre diferentes máquinas e reduzir variações que comprometessem testes, manutenção e acompanhamento do projeto.

Como a \emph{API} e o Painel Administrativo operam no ecossistema \emph{Node.js}, adotou-se a versão 20 ao longo da implementação. Para uniformizar essa base de execução, utilizou-se o \emph{Node Version Manager} (\emph{NVM}) \cite{nvmdocs}, com a definição da versão registrada no arquivo \texttt{.nvmrc}. Essa escolha favoreceu a consistência das rotinas de compilação, execução e verificação ao longo do trabalho.

A codificação foi realizada no \emph{Visual Studio Code}, utilizado como ambiente principal de trabalho pelo autor, com suporte à edição em \emph{TypeScript}, execução de comandos e acompanhamento integrado da base de código. Esse fluxo favoreceu ciclos curtos de implementação e revisão técnica.

A organização em repositórios separados para \emph{API}, Painel Administrativo e infraestrutura também contribuiu para a execução local do projeto. Enquanto os dois primeiros concentraram a evolução das funcionalidades, o repositório de infraestrutura reuniu a inicialização integrada dos serviços de apoio, favorecendo a reprodução do ambiente e o isolamento das responsabilidades técnicas de cada frente.

\section{Implementação da API}

Esta seção apresenta os principais fundamentos técnicos da implementação da \emph{API}, com foco na padronização das rotas, no processamento assíncrono de notificações e na documentação automatizada dos contratos. Esse conjunto contribuiu para manter consistência técnica, segurança de acesso e evolução contínua da aplicação.

\subsection{Padrão de codificação de rota}

A codificação das rotas foi organizada por um padrão único baseado em \emph{classes} que implementam a \emph{interface} \texttt{Route}. Em cada módulo, o método \texttt{getConfig()} descreve de forma declarativa os elementos de cada \emph{endpoint}: método HTTP, caminho, \emph{handler} e \emph{middlewares}. Essa abordagem manteve a definição funcional desacoplada do processo de registro no servidor.

A Figura~\ref{fig:info_routes_getconfig} exemplifica esse padrão no módulo de informações, evidenciando como as operações são descritas no próprio \texttt{getConfig()}, com regras de validação e autorização definidas por rota.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{imagens/rotas.png}
    \caption{Exemplo do padrão declarativo de rotas no método \texttt{getConfig()}}
    \label{fig:info_routes_getconfig}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

O registro efetivo dessas configurações foi centralizado em um componente de roteamento, responsável por percorrer automaticamente as classes de rota carregadas pelo contêiner de dependências e aplicar seus respectivos \emph{middlewares}. Dessa forma, novas funcionalidades puderam ser incorporadas sem alterações estruturais no mecanismo de registro.

Esse padrão também favoreceu o trabalho colaborativo entre as frentes de desenvolvimento. Enquanto as telas eram implementadas em paralelo no painel administrativo e no aplicativo móvel, a \emph{API} evoluía com contratos estáveis e reutilizáveis, reduzindo retrabalho e divergências de implementação entre clientes distintos.

A autenticação foi implementada com \emph{JSON Web Token} (\emph{JWT}) \cite{rfc7519jwt}. No fluxo de \texttt{/signin}, as credenciais são validadas e convertidas em um \emph{payload} com identificação e papel do usuário; em seguida, é emitido um \emph{token} utilizado nas requisições protegidas. Na camada de autorização, o \texttt{AuthMiddleware} decodifica o \emph{token} \emph{Bearer} e aplica controle de acesso por perfil, distinguindo operações restritas a \texttt{admin} das ações permitidas a \texttt{mom}.

Com isso, a padronização adotada para codificação de rotas combinou clareza declarativa, segurança de acesso e capacidade de evolução incremental, sustentando um ciclo de desenvolvimento mais fluido.

\subsection{Processamento assíncrono de notificações}

O processamento assíncrono foi adotado para desacoplar operações de notificação do ciclo de requisição \emph{HTTP}. Essa decisão foi orientada pelo crescimento esperado da plataforma, uma vez que o aumento do número de crianças cadastradas tende a elevar o volume de mensagens geradas. Nesse contexto, as respostas dos \emph{endpoints} permanecem rápidas, enquanto o envio é tratado em segundo plano.

Para o canal de \emph{push notification}, foi implementado um fluxo baseado em filas com \emph{BullMQ} e \emph{Redis} \cite{bullmqdocs,redisdocs}. A arquitetura de \emph{jobs} foi organizada em três etapas: preparação das mensagens elegíveis, envio ao provedor e conferência posterior de recebimento. Esse encadeamento permitiu distribuir a carga de processamento e reduzir o impacto de picos de envio sobre a camada de aplicação.

Na etapa de preparação, as notificações são personalizadas e agrupadas em lotes. Em seguida, o \emph{job} de envio registra o status de despacho de cada mensagem e encaminha os \emph{tickets} válidos para uma etapa de confirmação. Por fim, o \emph{job} de recebimento consulta os \emph{receipts} do provedor e persiste o status final, com política de retentativa para falhas transitórias \cite{expopushdocs}. Esse desenho viabiliza rastreabilidade completa entre envio e confirmação de entrega.

Além do fluxo programado, o mesmo mecanismo foi reutilizado em eventos de negócio pontuais, como resposta a dúvidas de usuárias. Assim, notificações acionadas por interação também seguem o \emph{pipeline} assíncrono e preservam o mesmo padrão de observabilidade operacional.

No canal de \emph{e-mail}, manteve-se envio assíncrono direto sem enfileiramento dedicado. Essa escolha foi adequada ao caráter pontual desses eventos, como avisos administrativos e códigos de verificação, evitando complexidade adicional para um volume de processamento significativamente menor.

A persistência dos estados de envio e recebimento foi concebida com lógica semelhante ao padrão \emph{Transactional Outbox} \cite{richardson2018microservices}, ao priorizar registro explícito dos resultados para auditoria e diagnóstico. Na prática, essa abordagem permitiu distinguir cenários como: mensagem enviada e recebida, mensagem enviada sem confirmação de recebimento e falha no provedor com detalhamento do erro. Como resultado, obteve-se um fluxo mais resiliente, observável e aderente à evolução da demanda.

\subsection{Documentação automatizada}

A descrição das rotas foi incorporada ao próprio fluxo de implementação, por meio da geração automática da especificação \emph{OpenAPI} e da publicação de uma tela interativa para consulta e validação \cite{swaggeropenapi}. Essa abordagem reduziu inconsistências entre o comportamento implementado e o conteúdo documentado.

A publicação ocorre em \texttt{/docs}, com uso da \emph{Swagger UI}, a partir de uma estrutura construída em tempo de execução. Nessa estrutura, os elementos de cada rota são derivados do padrão declarativo adotado em \texttt{getConfig()}, reaproveitando método, caminho, categoria e \emph{middlewares} definidos durante a codificação.

A composição automática também incorpora metadados de validação para \emph{body}, \emph{query} e \emph{params}, além da conversão de parâmetros de caminho para o formato da especificação. Em rotas protegidas, o esquema de segurança \emph{Bearer} é aplicado conforme a presença de autenticação na configuração da própria rota. Como resultado, os contratos publicados permanecem alinhados à implementação.

A Figura~\ref{fig:swagger_docs_promama} apresenta a visualização resultante desse processo, com organização por grupos e suporte à inspeção dos recursos disponíveis.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{imagens/swagger.png}
    \caption{Documentação automática das rotas na \emph{Swagger UI}}
    \label{fig:swagger_docs_promama}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

Em conjunto, a automação da documentação ampliou a confiabilidade do processo de evolução da \emph{API} e reforçou a colaboração entre as frentes de desenvolvimento, ao manter uma referência única, atualizada e aderente ao código-fonte.

\section{Prototipagem e Implementação do Painel Administrativo}

A prototipagem do Painel Administrativo foi conduzida como etapa prévia à implementação, com uso do \emph{Figma} como ferramenta de desenho e validação das telas \cite{figmadocs}. O objetivo central dessa etapa foi reduzir ambiguidades de construção e manter continuidade de uso em relação ao sistema anterior.

Para isso, foi adotada uma estratégia comparativa tela a tela, posicionando-se lado a lado a versão antiga e a versão redesenhada de cada módulo. Esse procedimento permitiu verificar, de forma incremental, a preservação de comportamentos essenciais e, simultaneamente, orientar ajustes de organização visual, hierarquia de informação e padronização de componentes.

Como resultado, foi consolidado um conjunto de protótipos que serviu de referência direta para a implementação do painel. Nas próximas subseções, serão apresentadas comparações entre telas correspondentes e uma descrição dos fluxos funcionais principais, com ênfase nos critérios adotados em cada módulo.

\subsection{Cadastro de informações, bairros e postos de saúde}

Os módulos de informações, bairros e postos de saúde foram concebidos a partir de uma mesma lógica de navegação e uso no painel administrativo. Para demonstrar essa diretriz comum, utilizou-se o módulo de informações como referência visual, por contemplar tanto a tela de listagem quanto a tela de cadastro.

No estado anterior do sistema, a listagem das informações seguia a organização apresentada na Figura~\ref{fig:tela_antiga_info}. Nessa versão, já estavam presentes ações centrais de consulta, edição e exclusão, mas a distribuição visual dos elementos ainda refletia a configuração do painel legado.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_old_info.png}
    \caption{Tela anterior de listagem do módulo de informações}
    \label{fig:tela_antiga_info}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

No processo de prototipagem, buscou-se consolidar, para a listagem, uma composição visual recorrente, composta por identificação clara do módulo, ação primária de criação em posição de destaque, campo de pesquisa, área central para consulta dos registros e paginação para navegação entre resultados. A Figura~\ref{fig:prototipo_novo_info} apresenta o protótipo dessa nova tela, elaborado no \emph{Figma} como referência também para os módulos de bairros e postos de saúde.

Em comparação com a versão anterior, observa-se uma hierarquia visual mais clara, com melhor separação entre busca, controle de paginação e ações por registro. Essa mudança tornou a leitura da tabela mais previsível e reforçou a organização de navegação adotada no painel redesenhado.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_new_info.png}
    \caption{Protótipo da nova tela de listagem do módulo de informações no \emph{Figma}}
    \label{fig:prototipo_novo_info}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

O mesmo raciocínio foi aplicado à tela de cadastro. A Figura~\ref{fig:tela_antiga_criacao_info} apresenta a versão anterior do formulário de criação de informações, cuja organização concentrava todos os campos em uma composição mais compacta, com menor destaque para agrupamentos funcionais e para o comando principal de salvamento.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_old_create_info.png}
    \caption{Tela anterior de cadastro do módulo de informações}
    \label{fig:tela_antiga_criacao_info}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

A Figura~\ref{fig:prototipo_novo_criacao_info} apresenta o protótipo da nova tela de cadastro no \emph{Figma}. Nessa proposta, os campos foram redistribuídos em sequência mais previsível, com melhor distinção entre título, faixa etária, corpo do texto, envio de imagem, links externos e vínculo com dúvidas frequentes, além de maior destaque para a ação de salvar.

Essa organização favorece o preenchimento progressivo do formulário e reduz a sobrecarga visual da tela. Embora a comparação tenha sido feita a partir do módulo de informações, a mesma base de organização foi replicada nos cadastros de bairros e postos de saúde, com adaptações restritas ao conteúdo de negócio de cada módulo.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_new_create_info.png}
    \caption{Protótipo da nova tela de cadastro do módulo de informações no \emph{Figma}}
    \label{fig:prototipo_novo_criacao_info}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

As Figuras~\ref{fig:implementacao_listagem_infos_painel_web} e~\ref{fig:implementacao_criacao_infos_painel_web} representam a etapa de implementação dessa proposta no painel web. Com elas, é possível verificar como as decisões definidas na prototipagem foram convertidas em uma solução efetiva, tanto na listagem quanto no fluxo de criação.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_impl_list_info.png}
    \caption{Tela implementada de listagem do módulo de informações}
    \label{fig:implementacao_listagem_infos_painel_web}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_impl_create_info.png}
    \caption{Tela implementada de cadastro do módulo de informações}
    \label{fig:implementacao_criacao_infos_painel_web}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

Tomadas em conjunto, as comparações de listagem e cadastro mostram que o módulo de informações funcionou como referência para a reorganização dos demais cadastros do painel. Mais do que uniformizar a aparência dessas telas, esse redesenho estabeleceu um arranjo reutilizável para bairros e postos de saúde, reduzindo retrabalho e favorecendo a manutenção de soluções equivalentes em contextos administrativos semelhantes.

\subsection{Fale conosco e dúvidas frequentes}

No módulo de \emph{Fale Conosco}, o redesenho não se limitou à renovação da listagem de mensagens. O principal objetivo foi aprimorar o fluxo de atendimento, de modo que a equipe identificasse imediatamente quais dúvidas ainda aguardavam resposta e, a partir desse ponto, pudesse decidir se cada mensagem deveria permanecer como atendimento individual ou ser incorporada à base pública de dúvidas frequentes.

A versão anterior é apresentada na Figura~\ref{fig:tela_antiga_doubts}. Nela, já existia a separação entre mensagens não respondidas e respondidas, com a alternância entre os conjuntos realizada por meio de um comando no canto superior direito. Ainda assim, a organização geral seguia a lógica do painel legado, com menor ênfase na hierarquia dos dados e na leitura rápida das pendências.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_old_doubts.png}
    \caption{Tela anterior do módulo \emph{Fale Conosco}}
    \label{fig:tela_antiga_doubts}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

No protótipo elaborado no \emph{Figma}, apresentado na Figura~\ref{fig:prototipo_novo_doubts}, essa lógica foi preservada, porém reorganizada com maior clareza na distribuição dos elementos. A tela passa a iniciar pela listagem de mensagens não respondidas, priorizando aquilo que demanda ação imediata da equipe, enquanto o botão de alternância permite exibir, sob demanda, o conjunto de mensagens já atendidas.

Essa decisão reforça o papel da tela como instrumento de triagem e acompanhamento. Em vez de tratar todas as mensagens de forma homogênea, a \emph{interface} coloca em primeiro plano o que ainda precisa de retorno, reduzindo a chance de uma dúvida permanecer sem resposta por falta de evidência mais clara para a equipe.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_new_doubts.png}
    \caption{Protótipo da nova tela do módulo \emph{Fale Conosco} no \emph{Figma}}
    \label{fig:prototipo_novo_doubts}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

A implementação final dessa proposta é apresentada na Figura~\ref{fig:implementacao_doubts}. Nela, observa-se a manutenção da prioridade para mensagens não respondidas, a presença do botão para alternância entre estados e a consolidação de colunas voltadas à identificação da usuária, ao conteúdo da dúvida, à resposta associada e à data de envio. Com isso, o painel passa a oferecer uma leitura mais objetiva do histórico de atendimento e das demandas ainda abertas.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_impl_doubts.png}
    \caption{Tela implementada do módulo \emph{Fale Conosco}}
    \label{fig:implementacao_doubts}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

Além da listagem, a funcionalidade se completa na tela de resposta apresentada na Figura~\ref{fig:implementacao_doubts_answer}. Nessa etapa, a equipe pode consultar a dúvida original, redigir a devolutiva e decidir se aquele conteúdo deve permanecer restrito à interação individual ou se possui relevância suficiente para ser compartilhado com todas as usuárias.

A opção \texttt{Responder dúvida para todos} materializa essa decisão. Quando marcada, a dúvida enviada por uma mãe deixa de ser apenas um registro do \emph{Fale Conosco} e passa a compor também o conjunto de dúvidas frequentes do sistema. Essa integração é importante porque transforma uma demanda inicialmente individual em conhecimento reutilizável, amplia o alcance da orientação registrada e reduz a recorrência de perguntas semelhantes no futuro.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_impl_doubts_anwser.png}
    \caption{Tela implementada de resposta a uma mensagem no módulo \emph{Fale Conosco}}
    \label{fig:implementacao_doubts_answer}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

Por fim, o redesenho desse módulo combinou organização da fila de atendimento e reaproveitamento do conhecimento produzido nas respostas. A priorização inicial das mensagens pendentes e a possibilidade de converter esse conteúdo em dúvidas frequentes reforçam o papel do painel não apenas como canal de suporte, mas também como meio de sistematização das informações mais recorrentes do programa.

\subsection{Notificações}

No módulo de notificações, a principal mudança não esteve no desenho da listagem, uma vez que essa tela segue a mesma base adotada nos demais cadastros do painel. O aspecto mais relevante dessa entrega foi a alteração da lógica de negócio: no sistema legado, as mensagens recorrentes dependiam de regras embutidas no código e não ofereciam um meio simples de ajuste pela equipe administradora.

A tela anterior, apresentada na Figura~\ref{fig:tela_antiga_notifications}, já permitia consultar os registros existentes, mas ainda refletia esse contexto herdado. Embora a estrutura geral contasse com tabela, ações e paginação, a gestão das mensagens permanecia limitada pela forma como os disparos haviam sido concebidos anteriormente, com pouca flexibilidade para adaptar conteúdo e recorrência.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_old_notifications.png}
    \caption{Tela anterior do módulo de notificações}
    \label{fig:tela_antiga_notifications}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

No protótipo desenvolvido no \emph{Figma}, mostrado na Figura~\ref{fig:prototipo_novo_notifications}, a tela foi alinhada ao \emph{design system} do painel, com campo de pesquisa, ação de criação em evidência, tabela central e paginação. Nesse caso, porém, o valor da prototipagem não estava em propor uma ruptura visual, mas em incorporar a administração das notificações ao mesmo fluxo de gestão já adotado nos demais módulos.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_new_notifications.png}
    \caption{Protótipo da nova tela do módulo de notificações no \emph{Figma}}
    \label{fig:prototipo_novo_notifications}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

A implementação final, apresentada na Figura~\ref{fig:implementacao_list_notifications}, evidencia essa transição. A listagem deixa de representar apenas mensagens genéricas e passa a expor registros efetivamente administráveis, com título, texto e faixa etária associada. Isso torna visível no painel uma decisão importante do trabalho: cada notificação recorrente passou a ser cadastrada como entidade própria na base de dados, em vez de permanecer dependente de comportamentos fixos no sistema legado.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/prototipo_impl_list_notifications.png}
    \caption{Tela implementada do módulo de notificações}
    \label{fig:implementacao_list_notifications}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

Essa modelagem permitiu cadastrar mensagens recorrentes conforme os marcos etários acompanhados pelo programa. Na prática, cada registro pode armazenar o conteúdo do \emph{push notification} e, opcionalmente, uma quantidade de dias que representa a faixa etária de destino. Assim, durante a execução diária do processamento assíncrono, o sistema relaciona a idade da criança à notificação correspondente e prepara o envio da mensagem adequada para aquele momento.

Também se tornou possível cadastrar notificações sem faixa etária definida, destinadas a comunicações de envio amplo. Nesse cenário, a mensagem deixa de depender de uma idade específica e passa a ser tratada pelo mesmo mecanismo de processamento. No \emph{back-end}, a consulta diária cruza as notificações cadastradas com os dados das crianças e utiliza o histórico de envio para evitar novos disparos bem-sucedidos para o mesmo destinatário.

Com isso, o ganho central desse módulo não foi apenas a atualização da listagem, mas a externalização de uma regra antes rígida para uma estrutura persistida e administrável. Ao transformar notificações recorrentes em dados configuráveis pelo painel, o projeto ampliou a capacidade de manutenção do sistema e tornou mais viável ajustar mensagens, faixas etárias e comunicações gerais sem depender de novas alterações no código-fonte.

\section{Testes e validação}

Nesta etapa do trabalho, a validação automatizada concentrou-se no \emph{back-end}, por reunir evidências reprodutíveis sobre regras de negócio, integração entre camadas e comportamento das rotas expostas pela \emph{API}. O objetivo desses testes foi reduzir regressões durante a implementação e oferecer uma base verificável para a evolução do sistema.

Para isso, foram adotadas duas estratégias complementares: testes unitários e teste integrado. A execução foi organizada de forma automatizada por meio do comando \texttt{npm run test:coverage}, que dispara as suítes implementadas e consolida, ao final, o relatório de cobertura do código. Com esse fluxo, tornou-se possível repetir a validação com o mesmo procedimento ao longo da evolução do projeto.

Os testes unitários foram distribuídos entre regras da camada de aplicação e componentes de infraestrutura. Na camada de aplicação, os cenários se concentraram em operações centrais e no tratamento de exceções, com substituição das dependências por objetos simulados para isolar a regra analisada. Já na infraestrutura, a validação se voltou principalmente aos repositórios, verificando o comportamento esperado da persistência e o contrato de acesso aos dados.

O teste integrado, por sua vez, foi implementado para exercitar a aplicação em condições mais próximas do ambiente real. Para isso, a execução inicia os serviços auxiliares definidos em \texttt{docker-compose.debug.yml}, cria uma base temporária com nome aleatório, aplica a migração inicial do esquema, inicializa a aplicação e envia requisições \emph{HTTP} à rota avaliada. Após cada caso, a base é limpa, e, ao final da suíte, o banco temporário é removido, assegurando isolamento entre execuções.

No estado atual do projeto, essa abordagem integrada foi aplicada ao fluxo de cadastro inicial de usuária, validando a rota \texttt{/api/onboard}. O teste confirma o código de resposta, a estrutura do objeto retornado e propriedades relevantes do cadastro persistido, como papel da usuária, campos nulos esperados e transformação segura da senha. Embora esse escopo ainda seja reduzido, ele já acrescenta uma camada de validação fim a fim ao conjunto automatizado do \emph{back-end}.

Os resultados obtidos na execução mais recente foram satisfatórios. A suíte unitária concluiu 142 casos com sucesso, enquanto o teste integrado executado concluiu 1 caso sem falhas, totalizando 143 verificações aprovadas. No relatório de cobertura, foram alcançados 79{,}61\% de \emph{statements}, 86{,}87\% de \emph{branches}, 86{,}66\% de \emph{functions} e 79{,}61\% de \emph{lines}, conforme apresentado na Figura~\ref{fig:coverage_back}.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/coverage_back.png}
    \caption{Relatório de cobertura dos testes automatizados do \emph{back-end}}
    \label{fig:coverage_back}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

Além dos percentuais globais, o relatório indica cobertura elevada em parte importante das regras centrais da \emph{API}, sobretudo na camada de aplicação. Ao mesmo tempo, o conjunto atual também evidencia uma oportunidade de evolução: ampliar os cenários integrados e estender a validação fim a fim para outras rotas, de modo a complementar a boa cobertura já alcançada pelos testes unitários.

\section{Implantação}

Esta seção apresenta as principais decisões adotadas para publicação do sistema, com foco na orquestração dos serviços, na incorporação dos dados herdados e na automação da entrega. O objetivo é mostrar como a infraestrutura foi organizada para sustentar a execução integrada da \emph{API} e do Painel Administrativo.

\subsection{Orquestração dos contêineres}

A implantação do conjunto de serviços foi centralizada no repositório \texttt{promama-infra}, responsável por concentrar a definição da infraestrutura, das variáveis de ambiente e da composição dos contêineres. Com essa decisão, a publicação do sistema deixou de depender de procedimentos isolados em cada aplicação e passou a ser conduzida a partir de um único ponto de orquestração.

No arquivo \texttt{docker-compose.yml}, essa organização foi materializada por meio dos serviços \texttt{promama\_api}, \texttt{promama\_admin}, \texttt{gateway}, \texttt{postgres}, \texttt{redis} e \texttt{adminer}, todos conectados à rede \texttt{promama\_net}. A composição também foi preparada para construir a \emph{API} e o Painel Administrativo a partir de seus respectivos repositórios, com caminhos configuráveis por variáveis de ambiente. Isso favoreceu tanto a reprodução local da arquitetura quanto sua execução em ambiente de servidor.

No caso da \emph{API}, a imagem foi configurada para compilar a aplicação, executar as migrações pendentes e iniciar o serviço em modo de produção. No Painel Administrativo, o contêiner realiza a geração do artefato estático e o publica por meio de um servidor web simples. Já o \emph{gateway} atua como ponto de entrada da solução, encaminhando requisições para a \emph{API} no caminho \texttt{/api} e para o painel na rota principal, além de concentrar o tratamento de HTTPS e o redirecionamento do tráfego em porta 80 para 443.

A Figura~\ref{fig:infra_docker_desktop} apresenta esse conjunto em execução no Docker Desktop, evidenciando a ativação coordenada dos serviços necessários ao funcionamento do ambiente implantado.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{imagens/docker_desktop.png}
    \caption{Contêineres da infraestrutura do Pró-Mamá em execução}
    \label{fig:infra_docker_desktop}
    \legend{Fonte: Elaborado pelo autor (2026).}
\end{figure}

Além da execução dos serviços principais, a composição também contemplou aspectos de apoio à operação. O volume \texttt{./files:/app/promama-api/public} foi utilizado para preservar arquivos públicos manipulados pela \emph{API}, enquanto o \emph{gateway} recebeu volumes específicos para certificados e configuração de acesso seguro. Com isso, a orquestração passou a abranger não apenas a inicialização dos processos, mas também elementos necessários à persistência de arquivos e à exposição controlada da aplicação.

\subsection{Migração dos dados retroativos}

O processo de migração foi incorporado ao fluxo de inicialização da \emph{API}. No contêiner dessa aplicação, o comando de entrada executa a sequência de compilação, migração e inicialização do servidor, garantindo que o banco de dados esteja preparado antes do início do atendimento das requisições. Essa decisão reduziu a dependência de passos manuais após a subida da infraestrutura.

Na implementação da rotina de migração, o arquivo \texttt{migrate.ts} cria a base de dados quando necessário, percorre os arquivos SQL da pasta \texttt{migrations} e registra cada migração concluída na tabela \texttt{migrations}. Com isso, o processo passa a respeitar a ordem numérica dos arquivos e evita a reexecução de scripts já aplicados, favorecendo continuidade entre diferentes implantações e atualizações do sistema.

Para a recuperação dos dados legados, foi utilizado o arquivo \texttt{01-promama\_dump\_2025\_02\_26.sql}, que concentra a carga retroativa extraída do sistema anterior. Diferentemente da migração estrutural inicial, esse arquivo atua como carga histórica, reinserindo registros já existentes, como bairros, postos e usuários previamente cadastrados. Na sequência, outros scripts complementam a base com dados institucionais e com estruturas introduzidas no novo ciclo de desenvolvimento, como termos atualizados e o módulo de notificações.

Esse mesmo cuidado foi adotado para os arquivos associados ao sistema anterior. A documentação do repositório de infraestrutura orienta que os recursos necessários sejam colocados previamente na pasta \texttt{promama-infra/files}, a qual é montada no diretório público da \emph{API}. Dessa forma, a migração retroativa não se limitou ao banco de dados, mas considerou também a preservação dos arquivos necessários para manter consistência entre os registros importados e os recursos acessados pela aplicação.

\subsection{Configuração do GitHub Actions}

A automação do processo de entrega foi distribuída entre os repositórios \texttt{promama-api} e \texttt{promama-admin}, ambos com \emph{workflows} de implantação configurados no GitHub Actions. Nessas rotinas, cada envio para a ramificação \texttt{main} aciona uma conexão SSH com o servidor e executa o comando \texttt{docker compose up -d --build} dentro do diretório \texttt{promama-infra}. Na prática, isso faz com que a atualização de qualquer uma das aplicações reutilize o mesmo ponto central de orquestração definido para a infraestrutura.

Essa configuração também evidencia uma separação de responsabilidades entre os repositórios. Enquanto \emph{API} e painel mantêm seus próprios fluxos de desenvolvimento e versionamento, o repositório de infraestrutura permanece como referência única para a publicação coordenada dos serviços. Essa escolha favoreceu rastreabilidade operacional e evitou a duplicação de scripts de implantação em múltiplos lugares.

No caso da \emph{API}, a automação não se restringiu à entrega. O repositório também possui um \emph{workflow} de testes executado em \emph{pull requests} para a ramificação \texttt{main}, no qual a infraestrutura de depuração é iniciada, a disponibilidade de PostgreSQL e Redis é verificada e, em seguida, a suíte automatizada é executada. Embora esse fluxo pertença ao contexto de integração contínua, sua presença reforça a confiabilidade do processo de implantação, pois introduz uma etapa de validação antes da atualização do ambiente publicado.

Em conjunto, a implantação do Pró-Mamá passou a combinar orquestração centralizada, migração controlada dos dados herdados e automação básica de entrega contínua. Esse arranjo tornou a publicação mais previsível e criou uma base de infraestrutura coerente com a evolução conjunta da \emph{API} e do Painel Administrativo.
